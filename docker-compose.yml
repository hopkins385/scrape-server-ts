name: scrape_server

services:
  app:
    container_name: scrape_server
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /app
    restart: always
    env_file:
      - .env
    security_opt:
      - no-new-privileges:true
